# 智绘王屋行 · 从零开始开发指南

本文档面向从零搭建、运行本小程序的开发者，说明项目是什么、用到了哪些技术，以及前后端与数据库需要安装的软件与环境配置。

---

## 一、项目是啥

**智绘王屋行** 是一款基于 **微信小程序** 的 **王屋山景区导览与攻略应用**，主要功能包括：

- **首页**：轮播图、天气、今日景区客流、景点/民宿推荐卡片、地图等  
- **风景页**：核心人文景点、自然生态、地质登山、道教文化、季节限定等分类介绍  
- **民宿推荐**：精选民宿列表、详情、预订入口  
- **攻略页**：旅行路线、美食与民宿推荐、预算问答等  
- **AI 页**：智能问答（愚仔）、支持文字与图片识别（调用智谱 GLM-4V 等）  
- **团队页**：团队相关功能  
- **我的**：个人中心等  

技术上采用 **微信小程序 + 微信云开发**，没有自建独立后端服务器，云函数和云数据库均在微信云开发环境中运行。

---

## 二、整体技术架构

| 角色       | 技术栈说明 |
|------------|------------|
| **前端**   | 微信小程序（WXML + WXSS + JavaScript），在微信开发者工具中开发与调试 |
| **后端**   | 微信云开发 · 云函数（Node.js），如 `cloudfunctions/chatImage`（AI 识图） |
| **数据库** | 微信云开发 · 云数据库（NoSQL），可选；当前部分数据为前端静态/配置数据 |
| **其他**   | 腾讯地图 API、图床（如 picui、gitee）用于图片资源；字体等通过 CDN 加载 |

因此：**不需要单独安装 MySQL、Redis 或自建后端服务**，只要安装下面几类软件即可从零跑通项目。

---

## 三、从零开始需要安装的软件

### 1. 前端（小程序开发与预览）

| 软件             | 用途                     | 说明 |
|------------------|--------------------------|------|
| **微信开发者工具** | 编辑、编译、预览、上传小程序 | 必装。[下载](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html) 稳定版即可。 |

- 用微信扫码登录，用已有小程序的 AppID（或使用测试号）打开本项目目录：`智绘王屋行`（即包含 `app.js`、`app.json` 的那一层）。
- 在「详情 → 本地设置」中可勾选「不校验合法域名」便于本地调试（正式发布前需在微信公众平台配置 request 合法域名）。

### 2. 后端（云函数）

| 软件     | 用途                     | 说明 |
|----------|--------------------------|------|
| **Node.js** | 云函数本地运行、依赖安装 | 建议 LTS 版本（如 18.x / 20.x）。[官网](https://nodejs.org/) 下载安装。 |

- 云函数目录：`智绘王屋行/cloudfunctions/`，例如 `chatImage`（AI 识图）依赖 `wx-server-sdk`、`axios` 等，需在该目录下执行 `npm install`。
- 在微信开发者工具中可右键云函数目录选择「在终端中打开」后执行 `npm install`，或本机已装 Node 时在对应目录下执行。
- 云函数部署：在微信开发者工具中右键对应云函数 → 「上传并部署：云端安装依赖」。

**不强制要求**：若只做前端页面与静态数据，不启用 AI 识图等云函数，可不装 Node.js；要用云函数则建议安装。

### 3. 数据库

本项目使用 **微信云开发自带的云数据库**，无需在本机安装 MySQL、MongoDB 等：

- 在微信开发者工具中点击「云开发」进入控制台，开通云开发并创建环境（如 `cloud1-xxx`）。
- 在 `app.js` 中已通过 `wx.cloud.init({ env: 'cloud1-9gmowq9c924cc435' })` 指定云环境，请与你在控制台创建的环境 ID 一致。
- 若后续需要存储用户数据、配置等，可在云开发控制台里新建集合、配置权限，在小程序或云函数中通过 `wx.cloud.database()` / 云函数内 `cloud.database()` 访问。

**天气功能说明**：
- 首页天气数据通过 **Open-Meteo 免费API** 实时获取，无需注册 API Key
- 需在微信公众平台配置 request 合法域名：`https://api.open-meteo.com`
- 若暂未配置域名，本地调试时可勾选"不校验合法域名"，天气会显示默认数据

**总结**：数据库 = 微信云开发控制台里使用，无需额外安装数据库软件。

### 4. 可选：版本管理与编辑器

| 软件           | 用途           | 说明 |
|----------------|----------------|------|
| **Git**        | 代码版本管理   | [下载](https://git-scm.com/) 安装后可在命令行或 IDE 中使用。 |
| **VS Code / Cursor** | 编辑 WXML/WXSS/JS | 可按需安装，微信开发者工具也自带编辑器。 |

---

## 四、推荐安装顺序（从零到跑通）

1. 安装 **微信开发者工具**，用 AppID 打开项目目录 `智绘王屋行`，先能编译、预览小程序界面。  
2. 开通 **云开发**，在控制台创建环境，保证 `app.js` 中 `env` 与该环境 ID 一致。  
3. 若使用 AI 识图等云函数：安装 **Node.js**，在 `cloudfunctions/chatImage` 下执行 `npm install`，再在开发者工具中上传并部署该云函数；在云函数环境变量中配置智谱 API Key（如 `ZHIPU_API_KEY`）。  
4. 需要云数据库时：在云开发控制台新建集合、配置权限，在代码中调用 `wx.cloud.database()` 即可，无需在本机安装任何数据库软件。

---

## 五、小结表

| 类型     | 需要安装的软件/环境     | 是否必装 |
|----------|--------------------------|----------|
| 前端     | 微信开发者工具          | 必装     |
| 后端     | Node.js（云函数开发/部署） | 使用云函数时建议装 |
| 数据库   | 无（使用微信云开发 · 云数据库） | 无需安装 MySQL 等 |
| 可选     | Git、VS Code / Cursor    | 按需     |

按上述步骤即可从零在本机跑通「智绘王屋行」小程序的前端、云函数与云数据库；若后续改为自建后端（如 Node/Java + MySQL），再另行安装对应运行环境与数据库即可。
