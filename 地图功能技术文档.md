# 智绘王屋行小程序地图功能技术文档

## 目录

1. [概述](#1-概述)
2. [技术选型](#2-技术选型)
3. [数据结构设计](#3-数据结构设计)
4. [核心功能实现](#4-核心功能实现)
5. [性能优化策略](#5-性能优化策略)
6. [辅助功能](#6-辅助功能)
7. [技术栈总结](#7-技术栈总结)
8. [功能亮点](#8-功能亮点)
9. [优化方向](#9-优化方向)

---

## 1. 概述

智绘王屋行小程序是一款基于微信小程序的智慧旅游应用，核心功能是交互式地图导览。地图模块承载了景点展示、路线导航、位置定位等核心交互功能，是用户感知最直接、使用最频繁的功能模块。

### 1.1 功能特性

智绘王屋行地图功能包含以下核心模块：

| 功能模块 | 说明 |
|---------|------|
| 景点标记 | 在地图上标注景点位置，点击查看详情 |
| 路线导航 | 三种游览路线可视化展示 |
| 智能缩放 | 根据缩放级别动态调整标签显示状态 |
| 视野适配 | 自动调整地图视野，包含路线和景点 |
| 位置定位 | 获取用户当前位置，提供导航起点 |
| 天气集成 | 显示实时天气和日出日落时间 |

---

## 2. 技术选型

### 2.1 核心技术框架

智绘王屋行小程序使用微信原生的 map 组件作为地图渲染引擎，这是最轻量、性能最优的方案。

地图组件接收的参数包括：

| 参数 | 类型 | 说明 |
|------|------|------|
| id | String | 地图唯一标识 |
| longitude | Number | 中心经度 |
| latitude | Number | 中心纬度 |
| scale | Number | 缩放级别 |
| markers | Array | 标记点数组 |
| polyline | Array | 折线数组 |
| circles | Array | 圆形区域 |
| subkey | String | 腾讯地图密钥 |
| show-location | Boolean | 显示当前位置 |
| enable-poi | Boolean | 启用 POI |
| enable-building | Boolean | 启用建筑物 |
| enable-3D | Boolean | 启用 3D 效果 |
| show-compass | Boolean | 显示指南针 |
| enable-zoom | Boolean | 启用缩放 |
| enable-scroll | Boolean | 启用拖拽 |

### 2.2 技术优势

智绘王屋行选择微信原生地图组件的原因：

| 优势项 | 说明 |
|-------|------|
| 原生性能 | 微信原生组件，渲染性能最优 |
| 零依赖 | 无需引入第三方地图 JS 库 |
| 体积小 | 代码体积小，首屏加载快 |
| 生态集成 | 与微信定位、支付等服务深度集成 |
| 维护成本低 | 微信维护，API 稳定更新 |

### 2.3 地图上下文方法

地图上下文提供以下常用方法：

| 方法 | 说明 |
|------|------|
| getScale | 获取当前缩放级别 |
| getCenterLocation | 获取中心点经纬度 |
| moveToLocation | 移动到指定位置 |
| includePoints | 调整视野包含坐标点 |
| translateMarker | 平移标记点 |

---

## 3. 数据结构设计

### 3.1 景点标记数据结构

每个景点标记包含以下属性：

| 属性 | 说明 |
|------|------|
| id | 唯一标识，景点使用 1-999 编号 |
| longitude | 经度 |
| latitude | 纬度 |
| iconPath | 自定义图标路径 |
| width | 图标宽度 |
| height | 图标高度 |
| title | 标题 |
| callout | 气泡标签配置 |

callout 气泡配置参数：

| 参数 | 说明 |
|------|------|
| content | 标签内容，支持换行 |
| display | 显示方式，BYCLICK 或 ALWAYS |
| fontSize | 字体大小 |
| borderRadius | 圆角半径 |
| bgColor | 背景色 |
| color | 文字颜色 |
| padding | 内边距 |
| textAlign | 文本对齐方式 |
| anchorY | Y 轴锚点偏移 |
| shadow | 阴影 |

### 3.2 标记点类型区分

| 类型 | ID 范围 | callout.display | 说明 |
|------|---------|-----------------|------|
| 景点 | 1-999 | BYCLICK | 默认点击显示标签 |
| 民宿 | 1000+ | ALWAYS | 始终显示标签 |

### 3.3 路线数据结构

小程序定义三种游览路线，每条路线包含以下属性：

| 属性 | 说明 |
|------|------|
| id | 路线唯一标识 |
| name | 路线名称 |
| description | 路线描述 |
| polyline | 折线坐标点数组 |
| color | 路线颜色 |
| width | 线条宽度 |
| dottedLine | 是否虚线 |
| arrowLine | 是否显示箭头 |
| markers | 路线包含的景点 ID 列表 |
| distance | 路线总长度 |
| duration | 预计时长 |
| difficulty | 难度等级 |

### 3.4 三种游览路线

| 路线 | 颜色 | 特点 |
|------|------|------|
| 步行登山路线 | 蓝色 | 全程步行，感受自然风光 |
| 索道游览路线 | 绿色 | 乘坐索道，省力便捷 |
| 魔毯漂流路线 | 紫色 | 刺激体验，老少皆宜 |

### 3.5 折线配置参数

| 参数 | 类型 | 说明 |
|------|------|------|
| points | Array | 坐标点数组 |
| color | String | 线条颜色 |
| width | Number | 线条宽度 |
| dottedLine | Boolean | 是否虚线 |
| arrowLine | Boolean | 是否显示箭头 |
| borderColor | String | 边框颜色 |
| borderWidth | Number | 边框宽度 |

---

## 4. 核心功能实现

### 4.1 标记点初始化

景点标记和民宿标记分别��始化：

**景点标记初始化规则：**

- ID 从 1 开始编号
- 使用景点专用图标
- 标签默认点击显示
- 支持显示景点名称和描述

**民宿标记初始化规则：**

- ID 从 1001 开始编号
- 使用民宿专用图标
- 标签始终显示
- 颜色采用暖色调区分

### 4.2 标签显示控制

根据缩放级别动态调整标签显示策略：

| 缩放级别 | 显示策略 | 说明 |
|---------|---------|------|
| 3-15 | BYCLICK | 仅点击显示，减少视觉干扰 |
| 16-20 | ALWAYS | 始终显示，方便查看 |

缩放级别与视图范围对应关系：

| 级别 | 视图范围 |
|------|---------|
| 3 | 全国视图 |
| 5 | 省份视图 |
| 8 | 城市视图 |
| 10 | 区县视图 |
| 12 | 乡镇视图 |
| 14 | 街道视图 |
| 16 | 建筑视图 |
| 18 | 商铺视图 |
| 20 | 精确视图 |

### 4.3 路线切换

三种游览路线之间的切换流程：

1. **更新 UI 选中状态** - 高亮当前选中的路线
2. **设置折线数据** - 更新地图上的路线显示
3. **调整地图视野** - 自动包含整条路线
4. **高亮路线景点** - 突出显示当前路线经过的景点

### 4.4 地图交互事件

**标记点击事件：**

- 显示景点或民宿详情弹窗
- 移动地图中心到标记点位置
- 触发详情页面跳转

**地图区域变化事件：**

- 缩放结束时更新标签显示状态
- 采用防抖机制，避免频繁更新
- 延迟时间设置为 300 毫秒

**点击地图空白处：**

- 隐藏所有显示中的标签
- 重置为点击显示模式

### 4.5 视野控制

| 功能 | 说明 |
|------|------|
| includePoints | 自动调整视野包含坐标点，设置上下左右边距 |
| moveToLocation | 移动到指定位置，支持平滑动画 |
| resetMapView | 回到初始位置，重新初始化标记 |

---

## 5. 性能优化策略

### 5.1 标记点缓存

只缓存景点标记（ID 小于 1000），民宿标记实时渲染。这样设计的原因：

- 景点数量较多（20+个），缓存可减少重复计算
- 民宿数量较少，无需缓存
- 分离缓存策略降低内存占用

### 5.2 防抖节流

| 场景 | 延迟时间 | 说明 |
|------|---------|------|
| 缩放更新 | 300ms | 避免缩放过程中频繁更新标签 |
| 路线切换 | 500ms | 防止快速切换导致的渲染问题 |

### 5.3 按需渲染策略

| 条件 | 渲染策略 |
|------|---------|
| 缩放级别 < 16 | 标签仅点击显示 |
| 缩放级别 >= 16 | 标签始终显示 |

### 5.4 数据预处理

路线数据在加载时进行预处理：

- 计算每条路线的边界框
- 预计算视野调整参数
- 减少运行时计算量

---

## 6. 辅助功能

### 6.1 天气信息显示

使用 Open-Meteo 免费 API 获取实时天气数据，无需申请 API Key。

显示内容包括：

| 数据项 | 说明 |
|-------|------|
| temperature | 当前温度 |
| humidity | 相对湿度 |
| cloudCover | 云量 |
| weatherCode | 天气代码 |
| sunrise | 日出时间 |
| sunset | 日落时间 |
| uvIndex | 紫外线指数 |
| tempMax | 最高温度 |
| tempMin | 最低温度 |

### 6.2 位置定位

| 功能 | 说明 |
|------|------|
| getLocation | 获取用户当前位置 |
| authorize | 申请定位权限 |
| getSetting | 检查权限设置状态 |

定位采用 GCJ-02 坐标系，与腾讯地图保持一致。

---

## 7. 技术栈总结

| 技术 | 用途 | 必要性 |
|------|------|-------|
| 微信小程序框架 | 应用框架 | 必需 |
| map 组件 | 地图渲染 | 必需 |
| 腾讯地图 SDK | 地理服务 | 可选 |
| Open-Meteo API | 天气数据 | 可选 |
| 微信云开发 | 后端服务 | 可选 |

### 7.1 第三方依赖

| 依赖 | 说明 | 是否必需 |
|------|------|---------|
| 微信开发者工具 | 开发调试 | 是 |
| 腾讯地图 Key | POI 等高级功能 | 否 |
| Open-Meteo | 天气数据 | 否 |

### 7.2 客户端兼容性

| 客户端 | 兼容性 | 备注 |
|-------|-------|------|
| iOS 微信 | 完全支持 | 性能最优 |
| Android 微信 | 完全支持 | 部分机型 getScale 可能失效 |
| 开发工具 | 完全支持 | 调试方便 |

---

## 8. 功能亮点

| 亮点 | 说明 |
|------|------|
| 多路线导航 | 步行、索道、魔毯三种模式自由切换 |
| 智能视野 | 自动调整视野包含路线和景点 |
| 动态标签 | 根据缩放级别智能显示和隐藏标签 |
| 天气集成 | 实时天气信息、日出日落时间 |
| 点击交互 | 标记点击显示详情，自动移动地图中心 |
| 流畅动画 | 平滑的移动和缩放动画效果 |
| 离线缓存 | 标记数据本地存储，提升加载速度 |
| 防抖优化 | 避免频繁更新，保证操作流畅度 |

---

## 9. 优化方向

### 9.1 功能增强

| 优化项 | 说明 | 优先级 |
|-------|------|-------|
| 路线预览动画 | 按顺序播放路线经过的景点 | 中 |
| AR 导航 | 手机摄像头实景导航 | 低 |
| 智能路线规划 | 输入起点终点智能规划路线 | 高 |
| 热力图 | 显示游客密度分布 | 中 |
| 轨迹记录 | 记录用户游览轨迹 | 中 |

### 9.2 性能提升

| 优化项 | 说明 | 预期效果 |
|-------|------|---------|
| 懒加载标记 | 只加载视野内的标记点 | 减少内存占用 |
| 图片压缩 | 优化标记图标大小 | 加快首屏加载 |
| 数据预取 | 提前加载路线数据 | 提升切换体验 |

### 9.3 用户体验优化

| 优化项 | 说明 |
|-------|------|
| 降低标签阈值 | 将缩放阈值从 16 调整到 14，更早显示标签 |
| 增加加载动画 | 路线切换时添加进度动画 |
| 标记搜索 | 支持按名称搜索景点并定位 |

---

## 附录

### A. 地图组件完整属性

| 属性 | 类型 | 默认值 | 说明 |
|------|------|-------|------|
| longitude | Number | - | 中心经度 |
| latitude | Number | - | 中心纬度 |
| scale | Number | 16 | 缩放级别 |
| markers | Array | [] | 标记点 |
| polyline | Array | [] | 折线 |
| circles | Array | [] | 圆形 |
| controls | Array | [] | 控件 |
| polygons | Array | [] | 多边形 |
| subkey | String | - | 腾讯地图 Key |
| show-location | Boolean | false | 显示当前位置 |
| enable-poi | Boolean | false | 启用 POI |
| enable-building | Boolean | true | 显示建筑物 |
| enable-3D | Boolean | false | 3D 效果 |
| show-compass | Boolean | true | 显示指南针 |
| show-scale | Boolean | false | 显示比例尺 |
| enable-zoom | Boolean | true | 启用缩放 |
| enable-scroll | Boolean | true | 启用拖拽 |
| enable-rotate | Boolean | false | 启用旋转 |

### B. 常见问题

**Q: 为什么缩放时标签不显示？**

A: 可能的原因：

- 缩放级别未达到阈值（默认 >= 16）
- getScale 方法在当前版本不可用
- 标记点的 callout.display 配置不正确

**Q: 如何调整标签显示阈值？**

A: 修改 updateMarkersCallout 函数中的判断条件，将阈值从 16 调整为 14 或 15。

**Q: 路线不显示怎么办？**

A: 检查要点：

- currentPolyline 是否正确设置
- polyline 的 points 数组是否为空
- 经纬度坐标是否在有效范围内

---

文档版本：v1.0  
更新日期：2026-04-15  
维护团队：智绘王屋行开发组
